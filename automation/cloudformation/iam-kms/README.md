# Directory `iam-kms`

This directory is designated for Application's and Infrastructure's IAM Roles, Policies and KMS Keys related resource creations CloudFormation(s).

In your project automation, you would create a stack(s) for above mentioned resources and export the resources ARNs. Then use those across the automation.
